[project]
name = "patchbay-sdk"
version = "0.1.0"
description = "Python SDK for Patchbay Gateway"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.0",
]

[build-system]
requires = ["setuptools>=75.0"]
build-backend = "setuptools.backends._legacy:_Backend"
