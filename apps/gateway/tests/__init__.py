"""Patchbay Gateway test suite.

Test pyramid:
  unit/        — Isolated module tests (routing, adapters, guardrails, billing)
  integration/ — API endpoint tests with real DB/Redis
  e2e/         — End-to-end workflow tests (Phase 2)
"""
