"""Unit tests — isolated module tests without external dependencies.

Tests:
  test_routing_engine.py    — Routing engine strategy selection
  test_cost_strategy.py     — Cost-based routing calculations
  test_circuit_breaker.py   — Circuit breaker state transitions
  test_provider_registry.py — Auto-discovery and adapter registration
  test_guardrails.py        — PII redaction, jailbreak detection, pipeline
  test_billing.py           — Cost calculation precision
  test_mcp_translator.py    — MCP ↔ provider schema translation
"""
