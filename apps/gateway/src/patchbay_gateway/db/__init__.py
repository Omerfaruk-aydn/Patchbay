"""Database layer — SQLAlchemy models, Alembic migrations, and seed data.

Contains:
  models/     — 13 SQLAlchemy ORM models (organizations, projects, keys, etc.)
  migrations/ — Alembic migration scripts
  seed.py     — Initial data population (models, routes, policies)
  base.py     — Declarative base class
"""
