"""V1 API endpoints — OpenAI-compatible unified API surface.

Endpoints:
  /v1/chat/completions — Chat completions (streaming + non-streaming)
  /v1/messages        — Anthropic Messages API passthrough
  /v1/responses       — OpenAI Responses API passthrough
  /v1/models          — Model catalog
  /v1/embeddings      — Embedding endpoint
  /v1/keys            — Virtual key management
  /v1/usage           — Usage and cost queries
  /v1/mcp/servers     — MCP server management
  /v1/seed            — Initial data seeding
"""
